import { useMemo, useState, type DragEvent, type PointerEvent } from "react";
import { ArrowLeft, CalendarDays, CheckSquare, LoaderCircle, MousePointer2, PenLine, Type } from "lucide-react";
import { useNavigate, useSearchParams } from "react-router";
import { toast } from "sonner";
import { PageHeader } from "../../components/PageHeader";
import { Button } from "../../components/ui/button";
import { dispatchSignatureRequest } from "../../lib/api";

type FieldType = "SIGNATURE" | "TEXT" | "SIGNING_DATE" | "DATE" | "CHECKBOX";
type Field = { id: string; type: FieldType; page: number; x: number; y: number; width: number; height: number; fontSize: number; required: boolean; textAlign: "LEFT" | "CENTER" | "RIGHT"; dataLabel: string };
const palette: Array<{ type: FieldType; label: string; icon: typeof PenLine }> = [{ type: "SIGNATURE", label: "서명", icon: PenLine }, { type: "TEXT", label: "텍스트", icon: Type }, { type: "SIGNING_DATE", label: "서명한 날짜", icon: CalendarDays }, { type: "DATE", label: "날짜", icon: CalendarDays }, { type: "CHECKBOX", label: "체크", icon: CheckSquare }];
const sizeFor = (type: FieldType) => type === "SIGNATURE" ? { width: 14, height: 4 } : type === "CHECKBOX" ? { width: 3.5, height: 3.5 } : { width: 14, height: 3.5 };

function PdfPage({ page, fields, onDrop, onMove, onStart, selectedId }: { page: number; fields: Field[]; onDrop: (event: DragEvent<HTMLDivElement>, page: number) => void; onMove: (event: PointerEvent<HTMLDivElement>, page: number) => void; onStart: (event: PointerEvent<HTMLDivElement>, id: string) => void; selectedId: string | null }) {
  return <div className="mb-6"><p className="mb-2 text-center text-xs text-muted-foreground">페이지 {page}</p><div onDragOver={(event) => event.preventDefault()} onDrop={(event) => onDrop(event, page)} onPointerMove={(event) => onMove(event, page)} className="relative mx-auto w-fit max-w-full overflow-hidden bg-white shadow-lg"><img src={`/samples/signature-placement-page-${page}.png`} alt={`계약서 ${page}쪽`} className="block max-w-full" draggable={false} />{fields.map((field) => { const item = palette.find((entry) => entry.type === field.type)!; const Icon = item.icon; return <div key={field.id} onPointerDown={(event) => { event.currentTarget.setPointerCapture(event.pointerId); onStart(event, field.id); }} className={`absolute flex cursor-grab touch-none select-none items-center justify-center border-2 border-dashed bg-sky-100/90 text-[var(--ocean)] ${selectedId === field.id ? "border-[var(--navy)] ring-2 ring-[var(--ocean)]/30" : "border-[var(--ocean)]"}`} style={{ left: `${field.x}%`, top: `${field.y}%`, width: `${field.width}%`, height: `${field.height}%`, fontSize: `${field.fontSize}px` }}><Icon className="mr-1 size-3.5" /><span className="font-bold">{item.label}</span></div>; })}</div></div>;
}

export function SignatureFieldPlacementPage() {
  const navigate = useNavigate(); const [params] = useSearchParams(); const [fields, setFields] = useState<Field[]>([]); const [selectedId, setSelectedId] = useState<string | null>(null); const [dragging, setDragging] = useState<{ id: string; pointerId: number } | null>(null); const [sending, setSending] = useState(false);
  const addAt = (event: DragEvent<HTMLDivElement>, page: number) => { const type = event.dataTransfer.getData("application/x-busanlink-field") as FieldType; if (!palette.some((item) => item.type === type)) return; const rect = event.currentTarget.getBoundingClientRect(); const size = sizeFor(type); const x = Math.max(0, Math.min(100 - size.width, ((event.clientX - rect.left) / rect.width) * 100)); const y = Math.max(0, Math.min(100 - size.height, ((event.clientY - rect.top) / rect.height) * 100)); const id = crypto.randomUUID(); setFields((all) => [...all, { id, type, page, x, y, fontSize: 12, required: true, textAlign: "LEFT", dataLabel: id.slice(0, 8), ...size }]); setSelectedId(id); };
  const moveAt = (event: PointerEvent<HTMLDivElement>, page: number) => { if (!dragging || dragging.pointerId !== event.pointerId) return; const rect = event.currentTarget.getBoundingClientRect(); const field = fields.find((item) => item.id === dragging.id); if (!field) return; const x = Math.max(0, Math.min(100 - field.width, ((event.clientX - rect.left) / rect.width) * 100 - field.width / 2)); const y = Math.max(0, Math.min(100 - field.height, ((event.clientY - rect.top) / rect.height) * 100 - field.height / 2)); setFields((all) => all.map((item) => item.id === field.id ? { ...item, page, x, y } : item)); };
  const selected = fields.find((field) => field.id === selectedId); const update = (patch: Partial<Field>) => setFields((all) => all.map((field) => { if (field.id !== selectedId) return field; const next = { ...field, ...patch }; return { ...next, x: Math.min(next.x, 100 - next.width), y: Math.min(next.y, 100 - next.height) }; }));
  const payload = useMemo(() => fields.map((field) => ({ field_type: field.type, data_label: field.dataLabel, required: field.required, font_size: field.fontSize, text_align: field.textAlign, position: { page: field.page, x: Number((field.x / 100).toFixed(4)), y: Number((field.y / 100).toFixed(4)) }, size: { width: Number((field.width / 100).toFixed(4)), height: Number((field.height / 100).toFixed(4)) } })), [fields]);
  const send = async () => { const contractId = params.get("contractId"); const versionId = params.get("versionId"); if (!contractId || !versionId) { toast.error("계약 정보를 찾을 수 없습니다."); return; } setSending(true); try { await dispatchSignatureRequest(contractId, versionId, payload); toast.success("셀러가 업로드한 PDF와 입력란 위치를 모두싸인으로 발송했습니다."); } catch (error) { toast.error(error instanceof Error ? error.message : "발송에 실패했습니다."); } finally { setSending(false); } };
  return <div className="mx-auto max-w-[1320px]"><Button variant="ghost" className="mb-4" onClick={() => navigate(-1)}><ArrowLeft className="mr-1 size-4" />최종 승인으로</Button><PageHeader title="서명·입력란 배치" description="입력란을 PDF로 끌어 놓고, 오른쪽에서 크기와 속성을 조절하세요." /><div className="grid gap-6 lg:grid-cols-[220px_minmax(0,1fr)_300px]"><aside className="h-fit rounded-xl border border-border bg-card p-4"><h2 className="font-semibold">입력란 도구</h2><div className="mt-3 space-y-2">{palette.map((item) => { const Icon = item.icon; return <div key={item.type} draggable onDragStart={(event) => event.dataTransfer.setData("application/x-busanlink-field", item.type)} className="flex cursor-grab items-center gap-2 rounded-lg border p-3 text-sm"><Icon className="size-4" />{item.label}</div>; })}</div><p className="mt-4 text-xs text-muted-foreground"><MousePointer2 className="mr-1 inline size-3" />PDF 안으로 끌어 놓으세요.</p></aside><main onPointerUp={() => setDragging(null)} className="h-[76vh] overflow-y-auto rounded-xl border bg-muted/40 p-4 sm:p-8">{[1, 2, 3, 4].map((page) => <PdfPage key={page} page={page} fields={fields.filter((field) => field.page === page)} onDrop={addAt} onMove={moveAt} onStart={(event, id) => { setSelectedId(id); setDragging({ id, pointerId: event.pointerId }); }} selectedId={selectedId} />)}</main><aside className="h-fit rounded-xl border border-border bg-card p-4"><h2 className="font-semibold">필드 속성</h2>{selected ? <div className="mt-3 space-y-3 text-xs"><p className="font-semibold">{palette.find((item) => item.type === selected.type)?.label}</p><label className="block">너비 {selected.width}%<input className="mt-1 w-full" type="range" min="3" max="35" step="0.5" value={selected.width} onChange={(event) => update({ width: Number(event.target.value) })} /></label><label className="block">높이 {selected.height}%<input className="mt-1 w-full" type="range" min="2" max="15" step="0.5" value={selected.height} onChange={(event) => update({ height: Number(event.target.value) })} /></label><label className="flex justify-between">필수 입력<input type="checkbox" checked={selected.required} onChange={(event) => update({ required: event.target.checked })} /></label>{["TEXT", "DATE", "SIGNING_DATE"].includes(selected.type) && <><label className="block">글자 크기 {selected.fontSize}pt<input className="mt-1 w-full" type="range" min="8" max="24" value={selected.fontSize} onChange={(event) => update({ fontSize: Number(event.target.value) })} /></label><label className="block">정렬<select className="mt-1 w-full rounded border p-1" value={selected.textAlign} onChange={(event) => update({ textAlign: event.target.value as Field["textAlign"] })}><option value="LEFT">왼쪽</option><option value="CENTER">가운데</option><option value="RIGHT">오른쪽</option></select></label></>}<label className="block">데이터 라벨<input className="mt-1 w-full rounded border p-1" value={selected.dataLabel} onChange={(event) => update({ dataLabel: event.target.value })} /></label><Button variant="outline" className="w-full" onClick={() => setFields((all) => all.filter((field) => field.id !== selected.id))}>삭제</Button></div> : <p className="mt-3 text-sm text-muted-foreground">필드를 선택해 주세요.</p>}<Button className="mt-6 w-full" disabled={sending} onClick={() => void send()}>{sending ? <><LoaderCircle className="mr-2 size-4 animate-spin" />모두싸인 발송 중...</> : <><PenLine className="mr-1 size-4" />모두싸인 발송</>}</Button>{sending && <div className="mt-3 rounded-lg bg-muted p-3 text-center text-xs text-muted-foreground"><div className="mx-auto mb-2 h-1.5 w-full overflow-hidden rounded-full bg-border"><div className="h-full w-1/2 animate-pulse rounded-full bg-[var(--ocean)]" /></div>PDF 업로드 및 모두싸인 문서를 만드는 중입니다. 잠시만 기다려 주세요.</div>}</aside></div></div>;
}
